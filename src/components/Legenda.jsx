import React from "react"
import styled from "styled-components"

const Modelo = styled.span`
    font-size: 8pt;
    text-align: center;
`

export default function Legenda() {
    return <Modelo> feito com ❤️ por pdrusr. </Modelo>
}